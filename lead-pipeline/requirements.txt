# Python packages needed for this project
# Install with: pip install -r requirements.txt

# Step 1: Scraper
selenium>=4.27.0       # Controls a real Chrome browser (bypasses anti-bot protection)
beautifulsoup4==4.12.3 # Parses HTML to extract data (names, emails, etc.)

# Step 2: Website Builder
requests>=2.31.0       # For calling DeepSeek API (simpler than openai package)

# Step 3: Deployer (coming soon)
# httpx                 # For API calls to Netlify

# Step 4: SMS Sender (Twilio)
# twilio>=9.0.0         # For sending SMS to leads
